<article id="widget-importer-exporter">
	<div id="toc-widget-importer-exporter" class="toc-id"></div>
	<h3 id="widget-importer-exporter"><?php _e( 'Widget Importer & Exporter' ); ?></h3>

<p>
<?php printf( _( '%1$sWidget Importer & Exporter%1$s'), '<a href="https://wordpress.org/plugins/widget-importer-exporter/" target="_blank">', '</a>'); ?>
 <?php _e( 'is useful for moving widgets from one WordPress site to another and backing up the widgets.' ); ?>
</p>

<h5><?php _e( 'Importing' ); ?></h5>

<p>
	<?php _e( 'Importing is a matter of uploading an export file created by the plugin. The results of an import are shown in a nicely formatted table with an explanation of what happened with each sidebar and widget.
	Importation takes into consideration the sidebars not existing in the current theme (widgets imported as Inactive), widgets that already exist in the same sidebar (widgets not duplicated) and the widgets that are not supported by the site (widgets not imported).' ); ?>
</p>

<h5><?php _e( 'Exporting' ); ?></h5>

<p>
	<?php _e( 'Widget Importer & Exporter can create an export file (in JSON format with .wie extension) out of currently active widgets. This file can be imported into other sites using this plugin or used to restore the widgets to the same site later.' ); ?>
</p>

</article>